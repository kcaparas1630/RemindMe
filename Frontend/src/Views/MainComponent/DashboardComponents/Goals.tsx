/**
 * Still in progress. Planning to add a AI Agent that will generate the goals based on the user's tasks in the database.
 */
import { FC } from 'react';
const Goals: FC = () => {
    return (
        <div>
            <h1>Goals</h1>

        </div>
    );
};

export default Goals;
